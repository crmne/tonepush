/target
# Compiled probe binaries; the sources next to them are what matter.
/tools/hxsniff/click
/tools/midiprobe/midiprobe
/tools/usbprobe/usbprobe
docs/_site
docs/.jekyll-cache
docs/.bundle
docs/vendor

# Python bytecode from the capture-decoding tools.
__pycache__/
*.pyc

# Session handover notes: useful locally, not part of the project.
NEXT-SESSION.md

# Publishing credentials. Holds 1Password references rather than secrets, and
# is still not committed: a file named .env is the first thing anything looks in.
.env

/.cache/

/dist/
